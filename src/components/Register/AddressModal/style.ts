import styled from "styled-components";

export const AddressModalContainer = styled.div`
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.3);
  position: fixed;
  top: 0;
  left: 0;
  z-index: 3;
  display: flex;
  padding: 2rem;
  justify-content: center;
  padding-top: 6rem;
`;

export const AddressModalItem = {
  width: "650px",
  height: "500px",
};
