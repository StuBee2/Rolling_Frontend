import { QueryClient, useQueryClient } from "react-query";
import { useGetCompanyInfoIdQuery } from "../../queries/Company/company.query";
import { useCallback } from "react";
import { CommonIdParam } from "../../repositories/common.param";

// export function useDetailCompany() {
//   //   const test = useGetCompanyInfoIdQuery();
// }

const useDetailCompany = () => {};

export default useDetailCompany;
